<%= form_for(current_user.active_relationships.find_by(followed_id: @category.id),
             html: { method: :delete },
             remote: true) do |f| %>
  <%= button_tag(type: "submit", class: "waves-effect waves-light btn") do %>
    Dejar de seguir
  <% end %>
<% end %>

        <% provide(:title, "Todos los tags") %>
<%= render 'layouts/navigation' %>
<%= render 'shared/bottom_nav' %>
        <% if @tags.empty? %>
          <div class="page fullscreen valign-wrapper accent-color animated fadeinup">
            <div class="container">
              <div class="row">
                <div class="col s12 m8 offset-m2">
                  <div class="valign">
                    <div class="center-align">
                      <h1 class="title big">¯\_(ツ)_/¯</h1>
                    </div>
                    <div class="center-align p-20">
                      <div class="card-panel animated fadein delay-2">
                        <h2>No hay tags todavia!</h2>
                        <span>Se el primero en crear una Nota con Tags.</span>
                      </div>
                    </div>
                  </div>
                </div>
              </div>
            </div>
          </div> 
          <% else %>
          <div class="page animated fadeinup">
            <div class="container">
              <div class="row">
                <div class="col s12 m8 offset-m2">
                  <div class="tags">
                    <%= render @tags %>
                  </div>
                </div>
              </div>
            </div>
          </div>
        <% end %>
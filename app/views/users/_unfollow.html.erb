<%= form_for(current_user.active_relationships.find_by(followed_id: @user.id),
             html: { method: :delete },
             remote: true) do |f| %>
  <%= f.submit "Dejar de seguir", class: "follow-button animated bouncein delay-6" %>
<% end %>

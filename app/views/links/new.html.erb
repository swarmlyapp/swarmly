      <% provide(:title, 'Crear nuevo enlace') %>
      <%= render 'shared/folder_toolbar' %>
      <%= render "shared/error_messages", object: @link %>
            <div class="page-folder animated fadeinup">
              <div class="container">
                <div class="row">
                  <div class="col s12 m6 offset-m3">
                    <div class="card form-inputs top-20 p-20">
                        <%= form_for(@link, url: group_links_path(@group)) do |f| %>
                            <div class="input-field animated fadeinright">
                                <%= f.text_field :title, class: "validate", placeholder: "Texto a mostrar" %>
                            </div>
                            <div class="input-field animated fadeinright">
                                <%= f.text_field :url, class: "validate", placeholder: "Pega aquí la url de tu enlace" %>
                            </div>
                            <div class="input-field animated fadeinright">
                                <%= f.text_area :content, class: "materialize-textarea", placeholder: "Contenido" %>
                            </div>

                            <%= f.hidden_field :user_id, value: current_user.id %>
                            <%= f.hidden_field :group_id, value: @group.id %>
                                
                            <%= f.submit "Crear enlace", class: "btn-waves-effect waves-light btn-large accent-color block animated bouncein delay-6", data: { disable_with: "Creando clip..." } %>
                        <% end %>
                    </div>
                  </div>
                </div>
              </div>
            </div> 
<%= render 'shared/folder_nav' %>